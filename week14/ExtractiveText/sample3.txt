in the current deployment of the microservice-based architecture we noticed that latency spikes are becoming more frequent especially during peak traffic hours which might be related to inefficient load balancing or improper caching strategy but it’s hard to isolate the exact root cause because the logging pipeline is fragmented across multiple monitoring tools and not all services are emitting structured logs consistently and some components are still using legacy synchronous calls instead of async which could be creating unnecessary blocking in the request lifecycle and additionally the database query optimizer seems to be generating suboptimal execution plans for large join operations probably due to outdated index statistics or missing composite indexes however the schema changes were rolled out incrementally without a full regression test suite so it’s unclear whether recent performance degradation is caused by code-level regressions or infrastructure misconfiguration and to make matters worse the CI/CD pipeline occasionally fails because dependency versions drift between environments resulting in non-reproducible builds which complicates debugging and slows down hotfix deployment and overall the system lacks proper observability, making it difficult to correlate metrics, traces, and error logs in a meaningful way.