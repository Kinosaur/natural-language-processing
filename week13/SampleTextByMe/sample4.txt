The microservice system is experiencing latency spikes during peak traffic, likely due to inefficient load balancing, caching issues, or legacy synchronous calls. Database performance is also degrading on complex queries, but identifying the root cause is difficult because our logging and observability tools are fragmented. Additionally, the deployment pipeline is unstable due to missing regression tests for schema changes and dependency mismatches in the CI/CD process.